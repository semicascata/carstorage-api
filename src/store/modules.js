import { store as auth } from '@/modules/authentication'
import { store as cars } from '@/views/cars'
import { store as navbar } from '@/components/layout/navbar'

export default {
  auth,
  navbar,
  cars
}
